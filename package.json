{
  "name": "toreholmberg.se",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && html-minifier-terser --collapse-whitespace --remove-comments --minify-css true --minify-js true index.html -o dist/index.html && cp -r public/. dist/",
    "deploy": "pnpm build && wrangler pages deploy ./dist"
  },
  "packageManager": "pnpm@10.32.1",
  "dependencies": {
    "wrangler": "^4.75.0"
  },
  "devDependencies": {
    "html-minifier-terser": "^7.2.0"
  }
}
